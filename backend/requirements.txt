fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
aiosqlite==0.20.0
alembic==1.13.1
pydantic[email]==2.7.1
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
httpx==0.27.0
pytest==8.2.0
pytest-asyncio==0.23.6

# Scraper fetcher: handles JS-rendered / anti-bot-protected sources (Sprint 17
# Phase 3). Installed from source per project decision, not the PyPI release.
# Pinned to the resolved commit for reproducibility.
#
# Known fragility, confirmed not worth chasing by bumping this pin: scrapling
# hardcodes a specific Chrome version (chrome_version in
# engines/toolbelt/fingerprints.py, "current versions hardcoded for now") and
# calls browserforge's HeaderGenerator with min_version == max_version at
# *import time*. browserforge's fingerprint dataset is fetched live and drifts
# — if it has zero samples for that exact hardcoded version, `import scrapling`
# raises ValueError. Reproduced directly against scrapling's `main` branch
# (chrome_version=149 there vs 148 here): same failure either way, so this is
# not a stale-pin problem, it's structural to how scrapling calls browserforge.
# scraper_service.py already treats scrapling as optional (see its
# module-level try/except) specifically because of this.
scrapling[fetchers] @ git+https://github.com/D4Vinci/Scrapling.git@c0f012d6625528a756e3852bad9ae7500c223579

# MVP Phase 1 property-test gate (workflow Step 1.3): random-spec invariant testing.
hypothesis==6.112.0
