/**
 * Shared editor-only palette.
 *
 * The workspace uses a cool blue-grey surround so the warm drawing sheet
 * reads as a deliberate architectural surface instead of another white card.
 * Room colours remain semantic and continue to come from the layout data.
 */
export const EDITOR_PALETTE = {
  workspaceHighlight: '#E7ECF2',
  workspaceStart: '#D6DEE8',
  workspaceEnd: '#B8C4D2',
  planSheetStart: '#FAF7F0',
  planSheetEnd: '#EEE8DB',
  planGrid: '#8290A3',
  planFrame: '#2B394E',
} as const
